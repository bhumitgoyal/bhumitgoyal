# Daily workflow needs only these two:
requests==2.32.3
beautifulsoup4==4.12.3

# Portrait pipeline — only runs locally when you change your photo.
# (Not installed by the daily GitHub Action.)
pillow==11.0.0
numpy==2.1.3
opencv-python-headless==4.10.0.84
rembg==2.0.59
